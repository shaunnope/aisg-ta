Individual response for two assessment questions.

## Text Generation
See Python script `textgen.py`. Remember to set environment variable `HF_TOKEN` before running script.

## Word Frequency
See Python script `wordfreq.py`.

# Requirements
Python 3.11.5
python-dotenv